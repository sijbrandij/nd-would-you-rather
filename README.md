- `create-react-app nd-would-you-rather`
- `cd nd-would-you-rather`
- `yarn install`
- `yarn start`
- move `_DATA.js` to `src/utils/` and create `helpers.js` and `api.js` for `formatQuestion` function and api calls

- 